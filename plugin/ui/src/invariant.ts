/**
 * 安装一致性校验（invariant）：包结构与 core 版本检查。
 * 参照 dsh-focus-chat 的 invariant 形态。
 */
export const name = 'dsh-market'

export function install() {
  return Promise.resolve()
}

export function verify() {
  return Promise.resolve()
}
