//#region src/invariant.ts
/**
* 安装一致性校验（invariant）：包结构与 core 版本检查。
* 参照 dsh-focus-chat 的 invariant 形态。
*/
const name = "dsh-market";
function install() {
	return Promise.resolve();
}
function verify() {
	return Promise.resolve();
}
//#endregion
export { install, name, verify };
